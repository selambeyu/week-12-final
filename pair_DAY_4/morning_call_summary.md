## Morning Call Summary — Day 4

Topic: **Evaluation and statistics — bootstrap CIs for benchmark comparisons.**

The first draft asked which bootstrap to use without forcing a **paired-data** definition of the eval. The call fixed the setup: **the same held-out tasks** generate **paired scores** \((a_i, b_i)\), so the estimand is a **mean of differences**, not independent means.

The question gained a **decision** frame: at plausible **Δ** and correlation **ρ**, when does **unpaired** resampling **widen** intervals enough to flip a **deploy** verdict? That pushed the explainer toward **variance of differences** and the dropped **covariance** term, not only a rule of thumb.

Anchors from **Week 11** (`Sales-Evaluation-Bench`, \(n=52\), illustrative \(\sigma\) and \(\rho\)) were written in so the post could show **width ratios** and a **numeric** deploy flip example.

Both partners attested the final questions were **diagnostic, grounded, generalizable, and resolvable** in ~800 words.
